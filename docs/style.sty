% A file for general settings

% packages for font
\usepackage{fontspec}
\usepackage{xeCJK}
\usepackage{unicode-math}
\setmathfont{latinmodern-math.otf}
\setmathfont[range=\setminus]{Asana-Math.otf}


\setsansfont[
  Path=/Users/sano/work/Noto_Serif_JP/,
  UprightFont=NotoSerifJP-Light,
  ItalicFont=NotoSerifJP-Light,
  BoldFont=NotoSerifJP-Bold,
  BoldItalicFont=NotoSerifJP-Bold,
  Extension = .otf
]{Noto_Serif_JP-Light}
\setmainfont[
  Path=/Users/sano/work/Noto_Serif_JP/,
  UprightFont=NotoSerifJP-Light,
  ItalicFont=NotoSerifJP-Light,
  BoldFont=NotoSerifJP-Bold,
  BoldItalicFont=NotoSerifJP-Bold,
  Extension = .otf
]{Noto_Serif_JP-Light}
\setCJKmainfont[
  Path=/Users/sano/work/Noto_Serif_JP/,
  UprightFont=NotoSerifJP-Light,
  ItalicFont=NotoSerifJP-Light,
  BoldFont=NotoSerifJP-Bold,
  BoldItalicFont=NotoSerifJP-Bold,
  Extension = .otf
]{Noto_Serif_JP-Light}



% These packages are needed for the custom theme "crystalline".
\usepackage{xcolor}
\usepackage{tikz}

\usetikzlibrary{positioning}
\usepackage{calc}

% We should not load the "graphicx" and the "color" package if we are using the loading
% above "sty/japanese.sty" since it is loading "jlisting", which loads "graphicx"
% and "color".
% However, you need to load this (if you want to insert images) when you omit the
% above "sty/japanese.sty".
%
% \usepackage[dvipdfmx]{graphicx}

\usepackage{forest}

\usepackage[
  sorting     = none,
  backend     = biber,
  maxbibnames = 2,
  doi         = false,
  isbn        = false,
  url         = false,
  eprint      = false,
]{biblatex}


% The references should be written in the "ref.bib".
\addbibresource{ref.bib} 

% The references should be written in the "ref.bib".
\addbibresource{ref.bib} 

% You need this to read our theme from the same directory.
\makeatletter
  \def\beamer@calltheme#1#2#3{%
    \def\beamer@themelist{#2}
    \@for\beamer@themename:=\beamer@themelist\do
    {\usepackage[{#1}]{\beamer@themelocation/#3\beamer@themename}}}
  \def\usefolder#1{
    \def\beamer@themelocation{#1}
  }
  \def\beamer@themelocation{}

% We are using the theme "crystalline" (my own theme) from the "theme" directory.
\usefolder{/Users/sano/work/express-beamer/theme}

% Here is the theme "crystalline".
% We have defined the "maincolor" and the "accentcolor" here.
% If you like, it maybe a good habit only to use these colors
% to create a sense of unity.
\usetheme{express} 




\usepackage{listings}
\definecolor{codebg}{gray}{0.85}
\lstset{
  backgroundcolor=\color{codebg},
  basicstyle={\ttfamily\small},
  frame={tb},
  breaklines=true,
  numbers=left,
  commentstyle={\smallitshape\color{black!80}},
%   identifierstyle={\small},
%   keywordstyle={\small\bfseries},
%   ndkeywordstyle={\small},
%   stringstyle={\small\ttfamily},
%   columns=[l]{fullflexible},
%   xrightmargin=0zw,
%   xleftmargin=1zw,
%   numberstyle={\scriptsize},
%   stepnumber=1,
%   numbersep=1zw,
%   lineskip=-1.5ex
}



